# Tasks — o1-watch

## Next action
UNSET -- set the current priority.

## In progress

## Backlog
- Refresh curated highlights/links on request (data.json is the source of truth).

## Done (last 10)
- Built self-contained `index.html` (opens via file://, no server/build): inline
  raw dataset in `<script id="data">`, Contributors + Latest posts + About views,
  search, per-person feed filter, influence/benchmark section. `./verify.sh` → VERIFY_OK.
